namespace Challenge;

/// <summary>
/// Given a string, return a description the number of letters and digits
/// in the format "LETTERS = X, DIGITS = Y".<br />
/// 
/// <b>Input constraints: </b><c>string</c> of length 1 .. 1000 characters.<br />
///
/// Output examples:
/// <code>
///     var c = new CountTheLettersAndDigits();
///     c.Count("Hello World!"); // expected "LETTERS = 10, DIGITS = 0"
///     c.Count("Today is 2022-01-01"); // expected "LETTERS = 7, DIGITS = 8"
///     c.Count("1234567890"); // expected "LETTERS = 0, DIGITS = 10"
/// </code><br/>
///
/// Note that spaces, punctuation, and special characters are not counted as
/// letters or digits.
/// </summary>
public class CountTheLettersAndDigits
{
    public string Count(string input)
    {
        /*** TODO: Implement me! */
        throw new NotImplementedException();
    }
}
